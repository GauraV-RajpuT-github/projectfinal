# synfront-main/.env

# The URL where your backend API lives inside Docker Compose
REACT_APP_BACKEND_URL=http://backend:5002

# (Optional) If your app reads a PORT variable to start
# your development server—you usually don’t need this in production,
# but it can be handy for local `npm start`:
PORT=3001
